# SPDX-License-Identifier: MPL-2.0
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lith-data
  labels:
    app: lith-http-api
    component: storage
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: fast-ssd  # Adjust based on cloud provider
  volumeMode: Filesystem
