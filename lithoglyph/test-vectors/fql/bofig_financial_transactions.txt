INPUT:
CREATE COLLECTION bofig_financial_transactions (
  investigation_id STRING NOT NULL,
  source_entity_id STRING NOT NULL,
  destination_entity_id STRING NOT NULL,
  amount INTEGER NOT NULL,
  currency STRING NOT NULL,
  transaction_date TIMESTAMP NOT NULL,
  instrument STRING,
  intermediary_entity_id STRING,
  source_account STRING,
  destination_account STRING,
  source_evidence_id STRING,
  notes STRING
)
WITH PROVENANCE {
  actor: "system:schema-init",
  rationale: "Bofig financial transactions — extracted from bank records, wire transfers, filings"
}
---
PARSED:
(create-collection "bofig_financial_transactions" (fields 12) (provenance (actor "system:schema-init") (rationale "Bofig financial transactions — extracted from bank records, wire transfers, filings")))
---
RESULT:
(ok (created "bofig_financial_transactions") (fields 12))
