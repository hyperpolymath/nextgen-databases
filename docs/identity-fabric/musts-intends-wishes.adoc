// SPDX-License-Identifier: CC-BY-SA-4.0
// SPDX-FileCopyrightText: 2026 Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= nextgen-databases — Normative Scope (Must / Intend / Wish)
:toc: preamble
:revdate: 2026-09-25

[abstract]
Per-repo normative-scope record for nextgen-databases (the coordination repo for
the database / query / identity layer, incubating `verisim-core/`). Estate rule
(2026-06-19): every repo carries its own scoped table; crossover content is
duplicated and marked until resolved, then wired into the other relevant repos.
`◇` marks proposals not yet canonical. Updated 2026-09-25 to reflect foundational
fixes for Issues #42, #45, #53.

== Confirmed stack placement (2026-06-19, still canonical 2026-09-25)

[literal]
krl (surface DSL) → tangle (TangleIR + type-safe core) → VerisimCore (categorical
abstraction — incubated here as verisim-core/) → Skein.jl / quandledb (storage +
equivalence)

== Levels

* *MUST* — required to be what it is (RFC 2119 normative).
* *INTEND* — committed direction.
* *WISH* — aspirational.

== nextgen-databases (own)

[cols="1,5", options="header"]
|===
| Level | nextgen-databases

| *MUST*
| Be the *coordination repo* for the database/query/identity layer (four families:
  VeriSimDB/VCL-UT, Lithoglyph/GNPL/Glyphbase, QuandleDB/KRL, Vocarium/Hermeneia);
  maintain `REGISTRY.adoc` as authoritative ownership map; enforce coordination-only
  via placement guard + block-db-writes hook; preserve `split-history/*` tags and
  `_split_*` branches (history-preserving extraction); estate governance — LICENSE/REUSE,
  SPDX headers, no banned files (TS/ReScript banned estate-wide, now 0 in coordination paths),
  K9 pedigree valid (7 files pass), trusted-base debt documented in `docs/proof-debt.adoc`;
  honest KRL-stack grounding (no op↔component category error).

| *INTEND* (2026-09-25 update)
| *DONE:* Pay down template debt — from 19 `{{PLACEHOLDER}}` files (2026-06-19) to 0
  (methodology.a2ml fixed 2026-09-25, `{{PROJECT_UNIQUE_STRENGTH}}` → real description).
  *DONE in coordination paths:* ReScript/TS/npm → AffineScript/Rust/Deno migration —
  0 `.res`/`.ts`/`package.json` in coordination paths via extraction (per-owning-repo
  migration continues). *DONE:* Give `verisim-core/` its own `AFFIRMATION.adoc` (shippable
  layer, 2026-09-25). *INTEND remaining:* Complete resite — extract remaining grandfathered
  dirs (`quandledb/` → `hyperpolymath/quandledb`, `nqc/` → `hyperpolymath/nqc`,
  `typeql-experimental/` → `hyperpolymath/vcl-ut`, `verisim-core/` → `hyperpolymath/VerisimCore.jl`,
  `verisim-modular-experiment/` → fold into `verisimdb`) per `RESITE-DATABASES-TO-OWN-REPOS.adoc`;
  shrink placement-guard grandfather list to empty; mature `verisim-core` toward shippable
  VerisimCore layer (persistence, concurrency, formal non-interference).

| *WISH*
| A coherent next-gen query surface (the `nqc` calculus) over the VerisimCore +
  Skein/QuandleDB substrate ◇; VerisimDB as a self-hostable identity-state store
  ◇; a clean separation of incubated experiments from shipped components — now
  enforced enduringly via placement guard + block-db-writes hook + `0-AI-MANIFEST.a2ml`
  coordination-only invariant. GNPL narration connected to live Lithoglyph journal
  with withdrawal across revisions ◇; QuandleDB/KRL explicit resolution witness ◇;
  VeriSimDB mandatory admissibility coverage for every advertised checked operation ◇.
|===

== Cross-repo wiring

nextgen-databases is the *incubator* — most notably the home of `verisim-core/`,
the VerisimCore categorical-abstraction layer that sits (maintainer-confirmed
2026-06-19) between TangleIR and the storage/equivalence faces. The shipped faces
of that layer live in their own repos: *Skein.jl* (engine), *quandledb*
(persistence + equivalence). The load-bearing shared invariant is
*equivalence-integrity* (the `a = a` idem core).

Sibling repos' key concerns (keep in sync as crossover resolves): *krl* — the
resolution DSL surface; *tangle* — TangleIR + type-safe core; *quandledb* —
equivalence / identity face; *Skein.jl* — computational engine; *echo-types* —
structured-loss theory + the identity-fabric framing (Conway·Fichte / Bruner /
Ricoeur); *KnotTheory.jl* — knot-theory mathematics library (Skein weak-dep);
*nextgen-typing* — type-theory coordination monorepo. See `../AFFIRMATION.adoc`.
