// SPDX-License-Identifier: PMPL-1.0-or-later
= EXPLAINME — verisim-modular-experiment
Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
:toc: left

This document explains, in plain terms, what this repo is and what question it answers.

== What is this?

A research experiment asking one question: *can VeriSimDB's eight-shape octad be split
into a slim identity core plus a federation of optional shapes, without breaking
VCL's consonance guarantees?*

This is NOT a replacement for VeriSimDB. Main `verisimdb/` remains canonical. This
experiment is a falsifier — either it proves the octad is divisible (producing a
useful `VerisimCore` abstraction) or it proves the octad is indivisible (producing
a formal justification of the monolith). Either outcome is acceptable.

== What question did it answer?

**Answer: the octad IS divisible.** A non-trivial identity core exists:

[cols="1,1,2",options="header"]
|===
|Classification |Shapes |Why

|Core (3 shapes)
|Semantic, Temporal, Provenance
|Store-level required. Semantic carries VCL's proof substrate. Temporal is required
 by the Identity Persistence invariant and the enrichment primitive. Provenance is
 the federation-trust substrate (hash-chain attestation).

|Federable (4 shapes)
|Vector, Tensor, Document, Spatial
|Absent shapes yield drift=0 by the `d(⊥,·)=0` convention in the formal model.
 Their absence makes query classes unavailable, not unsound.

|Conditional (1 shape)
|Graph
|Required only for cross-entity consonance claims (citations, lineage-edges).
 Omittable for per-entity workloads.
|===

Path B — the shape-absence hypothesis — is runtime-confirmed. 70 test assertions
green. Federated aggregate-drift numerically equals monolithic aggregate-drift over
the same data.

== How do I run it?

```bash
# Idris2 ABI — typechecks 4 modules (includes VCLProtocol.idr)
idris2 --build verisim-modular-experiment.ipkg

# Julia package test suite — 145 assertions across 6 suites via Pkg.test()
just test

# Zig CLI unit tests (verdict parser + protocol invariants)
just cli-test

# Build the Zig CLI binary
just cli-build-release

# Send a VCL query through the full CLI → Julia pipeline
just cli-query "PROOF INTEGRITY FOR abababababababababababababababababab"
```

Requirements: Julia 1.10+, Idris2 0.8.0, Zig 0.15+, libsodium (auto-fetched via Sodium.jl).

== Where is everything?

[cols="1,3",options="header"]
|===
|Path |Contents

|`README.adoc`
|Entry point: the research question + scope + relationship to main verisimdb.

|`PLAN.adoc`
|5-phase research plan with decision gates (Phase 0-3 closed, 4-5 pending).

|`PROOF-NEEDS.md`
|Central + subordinate proof obligations, and what's discharged.

|`src/Abi/`
|Idris2 ABI definitions — `Types.idr`, `VerisimCore.idr`, `FederationContract.idr`, `VCLProtocol.idr` (CLI stdio wire protocol).

|`src/Verisim.jl`
|Julia package entry point. Loads all impl modules into `Main` via `Base.include`, exposes thin delegators.

|`src/vcl_server.jl`
|Julia stdio server for the CLI consumer. Spawned by the Zig binary; reads VCL from stdin, proves via `Verisim` package, emits A2ML verdicts.

|`ffi/zig/`
|Zig CLI binary (`verisim`). Spawns `vcl_server.jl` as a subprocess; pipes queries through; forwards A2ML verdicts to stdout. `build.zig`, `build.zig.zon`, `src/verisim_cli.zig`.

|`impl/`
|Julia reference implementation — `VerisimCore.jl`, `FederationManager.jl`,
 `peers/VectorPeer.jl`, `drift/Metrics.jl`, `Crypto.jl`.

|`test/`
|Julia test suites — Core-only, federation-parity, seam-audit-fixes.

|`spec/federation-contract.adoc`
|Prose spec for the 5-clause federation contract.

|`docs/`
|Design notes, per-phase artefacts, session logs, seam audit, foldback log.

|`.machine_readable/6a2/`
|A2ML files: STATE, META, ECOSYSTEM (structured project metadata).

|`0-AI-MANIFEST.a2ml`
|Entry point for AI agents working in this repo.
|===

== The five federation-contract clauses

Any Federable peer must honour these for VCL claims to compose soundly:

. **Aggregate-drift weight renormalisation** — when a shape is absent, pair weights renormalise to sum=1 over present pairs.
. **Drift-signal projection** — peer exposes per-pair drift contributions, not just query results.
. **Attestation signatures** — every peer response is Ed25519-signed with a freshness window.
. **Coherence-constraint surface** — peer exposes projections for cross-shape coherence evaluation.
. **Conflict-resolution contract** — peer accepts Core's Temporal as authoritative LWW order.

See `spec/federation-contract.adoc` for details.

== What's deferred past Phase 3?

All three immediate follow-ups now complete:

* ✓ **Ed25519** — real Ed25519 via libsodium (Sodium.jl). `impl/Crypto.jl` +
  VerisimCore/VectorPeer/DocumentPeer all signed with real keypairs.
* ✓ **Multi-peer non-interference** — `impl/peers/DocumentPeer.jl` registered
  alongside VectorPeer. 15 assertions confirm drift pairs (S,V), (S,D), (V,D)
  compose; each peer has an independent Ed25519 keypair.
* ✓ **VCL parser/evaluator** — `impl/vcl/{Query,Prover,Parser}.jl`. Supports
  PROOF INTEGRITY, PROOF CONSISTENCY WITH DRIFT < θ (with OVER scope),
  PROOF FRESHNESS. 32 assertions.

All 5 phases + 3 deferred items complete. **Experiment closed, Path B confirmed.**

* ✓ **Phase 4 dogfood** — `examples/krladapter_integration.jl` +
  `test/test_krladapter_integration.jl` (19 assertions). KRLAdapter.jl
  client satisfied by Core alone — no Federable peers required.
* ✓ **Phase 5 findings** — `docs/FINDINGS.adoc` with classification,
  degradation routes, adoption guide, foldback tracking, and the
  productisation recommendation.

Open post-experiment work (not blocking):

* ✓ **Package**: `impl/` promoted to shippable `Verisim` Julia package (`src/Verisim.jl`, `Project.toml`, `test/runtests.jl`).
* ✓ **CLI consumer**: `ffi/zig/` — Zig binary + `src/vcl_server.jl` Julia server + `src/Abi/VCLProtocol.idr` Idris2 ABI. Subprocess architecture (OS-level isolation per estate priority order). 4/4 unit tests green.
* Formal Idris2-type-level non-interference proof at arbitrary N.
* **Phase 6 refactor** (optional): migrate `impl/` from flat-include scaffold to proper Julia `src/` with `using ..Sibling` — removes `Main.X` cross-references and `Base.include(Main, f)`.

== What if the result had been negative?

If Path B had failed — if no non-trivial Core existed — the experiment would have
produced a formal indivisibility argument folded back into main verisimdb's
documentation as justification for the octad-as-unit. That outcome would have been
equally valuable, just different.
