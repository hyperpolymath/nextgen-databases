// SPDX-License-Identifier: CC-BY-SA-4.0
// SPDX-FileCopyrightText: 2026 Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= TEST-NEEDS — nextgen-databases (Coordination Repo)
:toc: left
:revdate: 2026-09-25

== Status (2026-09-25) — Coordination Repo

This is a *coordination repository* (see `0-AI-MANIFEST.a2ml`, `REGISTRY.adoc`).
Per-database implementation and its tests now live in owning repositories.
This file tracks *cross-database integration tests* and *governance checks*
that belong in this repo, not per-database unit tests.

Historical CRG Grade C (2026-04-04) referenced verisimdb/lithoglyph Elixir/Rust/Gleam
tests that have been extracted to `hyperpolymath/verisimdb` and `hyperpolymath/lithoglyph`.
Those counts are not reasserted here; see owning repos' own TEST-NEEDS.

== Current State — What This Repo Tests

[cols="1,1,3", options="header"]
|===
| Category | Status | Notes

| Governance (MUST/TRUST) | PASS | `LICENSE` present, no Dockerfile/Makefile, SPDX headers, no `.env`
| K9 pedigree validation | PASS | `./.github/hooks/validate-k9.sh` — 7 files, 0 errors (2026-09-25)
| A2ML validation | PASS (2 warnings) | `./.github/hooks/validate-a2ml.sh` — 36 files, 0 errors, 2 SPDX warnings in ANCHOR.a2ml + 0-AI-MANIFEST.a2ml (pre-existing, not blocking)
| Placement guard | PASS | `.github/workflows/placement-guard.yml` — fails on new files outside coordination paths; lithoglyph/verisimdb now FAIL (not warn)
| Block-db-writes hook | PASS | `.claude/hooks/block-db-writes.sh` — blocks NEW file creation in legacy DB dirs
| Template residue | FIXED | `grep -R "{{PLACEHOLDER}}"` → 0 files (was 19 in 2026-06-19); methodology.a2ml fixed
| ReScript/TS migration | DONE in coordination paths | `find . -name "*.res" -o -name "*.ts"` → 0 files (was 143 + 4 package.json)
| Proof-debt | DOCUMENTED | `docs/proof-debt.adoc` exists, no TRUSTED/AXIOM/believe_me in coordination paths
| SPDX compliance | PASS | Sampled `.adoc`, `.a2ml`, `.jl`, `.sh` carry SPDX
| No banned patterns | PASS | `believe_me`, `Obj.magic`, `unsafeCoerce` → 0 in coordination paths
|===

== What Belongs Here vs Owning Repos

*This repo (coordination):*

- Cross-database integration tests (`tests/` — currently minimal, see below)
- Governance checks (Mustfile, Trustfile, K9, A2ML, placement guard)
- Evidence ledger checks (EXPLAINME.adoc boundaries)
- Registry consistency (REGISTRY.adoc links resolve)

*Owning repos (per REGISTRY.adoc):*

- `verisimdb` — E2E, P2P property, security, concurrency, benchmarks (was 40+12+18+5+10+14 tests in 2026-04-04 snapshot)
- `lithoglyph` — Zig core tests (27 tests in core-zig per EXPLAINME.adoc), beam smoke
- `gnpl` — Lean 4.15.0: `lake build` + `lake test` (5 suites, 35 narration checks, 26 private-substrate)
- `quandledb` — Julia: fragment corpus 11 checks, integration 112 checks, resolution-boundary 8
- `vcl-ut` (typeql-experimental) — Rust parser 36 tests with RUST_MIN_STACK
- `hermeneia` — 39 Rust tests + 7 e2e slice tests
- `vocarium` — 6 Zig tests + invoke_slice.sh
- `verisim-core` — Julia 55 assertions (core lifecycle, Ed25519, attestation, federation)

== Known Gaps (Coordination Repo)

- [ ] Cross-database integration tests in `tests/` — currently only placeholder structure; need at least one test that verifies REGISTRY.adoc links resolve and that pointer READMEs (lithoglyph/, verisimdb/) point to live repos
- [ ] `tests/fuzz/placeholder.txt` — historical fake-fuzz alert from rsr-template-repo template. File does not exist in current tree (verified via `find tests -type f`), but if reintroduced, must be real fuzz harness or removed. Original alert retained as historical record: placeholder inherited from template does NOT provide real fuzz testing
- [ ] Remote CI status — This file reports local checks only; no claim about GitHub Actions remote runs
- [ ] Sub-project builds — `julia`, `gleam`, `deno`, `zig` absent in this container; authoritative checks are `julia --project=verisim-core -e 'using Pkg; Pkg.test()'`, `gleam test` in `nqc/`, etc.

== Priority (2026-09-25)

*Governance and coordination checks PASS.* Template debt FIXED, ReScript/TS migration DONE in coordination paths, verisim-core AFFIRMATION exists, K9 pedigree and trusted-base documented. Remaining work is completing resite extraction of grandfathered legacy dirs (`quandledb/`, `nqc/`, `typeql-experimental/`, `verisim-core/`, `verisim-modular-experiment/`) per `RESITE-DATABASES-TO-OWN-REPOS.adoc` and adding cross-database integration tests.

== Verification

[source,bash]
----
# Governance
./.github/hooks/validate-k9.sh
./.github/hooks/validate-a2ml.sh

# Template residue
grep -R "{{PLACEHOLDER}}" --include="*.a2ml" --include="*.adoc" . | grep -v ".git" | wc -l   # expect 0
grep -R "{{PROJECT" --include="*.a2ml" . | grep -v ".git" | grep -v "reject-if-contains" | wc -l   # expect 0

# ReScript/TS
find . -name "*.res" -o -name "*.resi" -o -name "*.ts" -o -name "*.tsx" | grep -v ".git" | wc -l   # expect 0

# Banned patterns
grep -R "believe_me\|Obj.magic\|unsafeCoerce" --include="*.idr" --include="*.jl" --include="*.rs" docs/ .machine_readable/ tests/ | wc -l   # expect 0

# Proof-debt
test -f docs/proof-debt.adoc && echo "proof-debt documented"

# Verisim-core AFFIRMATION
test -f verisim-core/AFFIRMATION.adoc && echo "verisim-core AFFIRMATION exists"

# Disentanglement
test -f docs/migration/LITHOGLYPH-GNPL-DISENTANGLEMENT.adoc && echo "disentanglement documented"
----

All checks pass as of 2026-09-25.

== Historical — CRG Grade C (2026-04-04) for reference

Pre-extraction snapshot (verisimdb + lithoglyph still in monorepo):

- Unit ~40, Integration ~12, E2E 18, P2P 5 props+1 test, Security 10, Concurrency 14, lithoglyph smoke Gleam, Benchmarks 2 Rust files
- Source modules ~833 across 2 subsystems
- Known gaps: VCLTypeChecker `binary_to_existing_atom` ArgumentError, VCL parser null-byte, SchemaRegistry idempotency, kraft_node remove_server timeout
- FAKE-FUZZ ALERT: `tests/fuzz/placeholder.txt` inherited from rsr-template-repo — does NOT provide real fuzz testing — replace with real harness or remove

That work was recovered upstream via `hyperpolymath/verisimdb#219` (7 files, 43 tests, 0 failures) and preserved in `split-history/verisimdb` tag (713 files, 124 commits). See `verisimdb/README.adoc`.

== References

- Issue #42: Template debt + ReScript/TS migration + verisim-core AFFIRMATION
- Issue #45: Lithoglyph/GNPL/Glyphbase extraction
- Issue #53: K9 pedigree + trusted-base
- `docs/AFFIRMATION.adoc` (2026-09-25): Current honest state
- `docs/proof-debt.adoc`: Trusted-base inventory
- `docs/migration/LITHOGLYPH-GNPL-DISENTANGLEMENT.adoc`: Design decision
- `EXPLAINME.adoc`: Evidence ledger with executable boundaries
- `REGISTRY.adoc`: Ownership map
